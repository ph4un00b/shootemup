en mac:

- abrir terminal.app
- colocarse en la raíz del directorio del repositorio
- levantar un servidor local => $ python -m SimpleHTTPServer 5000
- ir a la página => http://localhost:5000/game.html